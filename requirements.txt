# This connector's driver only. The engine image ships no database drivers
# and provides the CDK itself, which is never listed here.

# SQLAlchemy dialect for ClickHouse. Registers the `clickhouse+asynch`
# entry point the transport names; 0.3.x is the SQLAlchemy 2.0 line.
clickhouse-sqlalchemy>=0.3.0

# Async ClickHouse DBAPI over the native TCP protocol - the `asynch` half
# of `clickhouse+asynch`. `secure` and `verify` are direct named parameters
# of asynch.proto.connection.Connection (confirmed in 0.2.4 source and 0.3.x
# source; neither is delegated through **kwargs). Floor verified against PyPI:
# 0.2.4 is the earliest patch where both TLS kwargs are confirmed present; the
# 0.3.x line is also compatible. Also drives the columnar block insert that
# makes executemany the connector's bulk path.
asynch>=0.2.4
